<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>spring-cloud-azure</artifactId>
        <groupId>com.microsoft.azure</groupId>
        <version>1.0.0.BUILD-SNAPSHOT</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <properties>
        <main.basedir>${basedir}/..</main.basedir>
    </properties>

    <artifactId>spring-cloud-azure-samples</artifactId>
    <name>Spring Cloud Azure Code Samples</name>
    <packaging>pom</packaging>

    <modules>
        <module>spring-cloud-azure-cache-sample</module>
        <module>spring-cloud-azure-config-sample</module>
        <module>spring-cloud-azure-storage-sample</module>
        <module>spring-cloud-azure-eventhub-operation-sample</module>
        <module>spring-cloud-azure-eventhub-integration-sample</module>
        <module>spring-cloud-azure-eventhub-binder-sample</module>
        <module>spring-cloud-azure-eventhub-kafka-sample</module>
        <module>spring-cloud-azure-servicebus-operation-sample</module>
        <module>spring-cloud-azure-servicebus-integration-sample</module>
        <module>spring-cloud-azure-servicebus-topic-binder-sample</module>
        <module>spring-cloud-azure-storage-queue-operation-sample</module>
        <module>spring-cloud-azure-storage-queue-integration-sample</module>
        <module>azure-messaging-sample</module>
    </modules>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-checkstyle-plugin</artifactId>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin>
        </plugins>

        <testResources>
            <testResource>
                <directory>src/test/resources</directory>
                <filtering>true</filtering>
            </testResource>
        </testResources>

    </build>

</project>
